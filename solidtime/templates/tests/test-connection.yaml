apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "solidtime.fullname" . }}-test-connection"
  labels:
    {{- include "solidtime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "solidtime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
