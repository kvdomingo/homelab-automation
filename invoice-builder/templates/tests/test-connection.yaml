apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "invoice-builder.fullname" . }}-test-connection"
  labels:
    {{- include "invoice-builder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "invoice-builder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
