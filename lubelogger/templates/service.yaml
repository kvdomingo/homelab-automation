apiVersion: v1
kind: Service
metadata:
  name: {{ include "lubelogger.fullname" . }}
  labels:
    {{- include "lubelogger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lubelogger.selectorLabels" . | nindent 4 }}
