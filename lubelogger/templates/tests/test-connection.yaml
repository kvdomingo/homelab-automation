apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lubelogger.fullname" . }}-test-connection"
  labels:
    {{- include "lubelogger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lubelogger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
