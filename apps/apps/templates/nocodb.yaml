{{- if not .Values.disableNocoDb }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nocodb
  namespace: {{ include "apps.argocdNamespace" . | quote }}
  finalizers:
    {{- include "apps.finalizers" . | nindent 4 }}
spec:
  project: {{ .Values.project }}
  source:
    repoURL: https://zekker6.github.io/helm-charts/
    chart: nocodb
    targetRevision: 1.7.0
    plugin:
      name: argocd-vault-plugin-helm
      env:
        - name: HELM_VALUES
          value: |
            image:
              tag: latest
            env:
              NC_DB: 'pg://10.10.10.103:5432?u=nocodb&p=<path:kv/data/lab/nocodb#POSTGRESQL_PASSWORD>&d=nocodb'
            ingress:
              main:
                enabled: true
                annotations:
                  gethomepage.dev/enabled: "true"
                  gethomepage.dev/name: "NocoDB"
                  gethomepage.dev/group: "Documents"
                  gethomepage.dev/icon: "nocodb.png"
                  gethomepage.dev/ping: "noco.{{ .Values.ingress.baseDomain }}"
                pathType: Prefix
                hosts:
                  - host: nocodb.{{ .Values.ingress.baseDomain }}
                    paths:
                      - path: /
                        pathType: Prefix
                  - host: nocodb.{{ .Values.ingress.externalDomain }}
                    paths:
                      - path: /
                        pathType: Prefix
            postgresql:
              enabled: false
              auth:
                postgresPassword: "<path:kv/data/lab/nocodb#POSTGRESQL_PASSWORD>"
                username: nocodb
                password: "<path:kv/data/lab/nocodb#POSTGRESQL_PASSWORD>"
                database: nocodb
  destination:
    server: {{ .Values.destination.server }}
    namespace: nocodb
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true
{{- end }}
